# The root build: the Java 8 core and the Chromium helper, on their own.
# Each Minecraft version's build is under versions/<minecraft>/ with its own properties.
java_version=25
org.gradle.jvmargs=-Xmx3G
org.gradle.daemon=false
